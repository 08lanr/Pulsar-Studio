// The window event the "This computer" card sends when the films folder
// changes, so the import list under it reads the new folder at once.
export const COMPUTER_CHANGED = "studio:computer-changed";
