"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { postJson } from "@/lib/api-client"; import type { Producer } from "@/lib/types";
export default function ProducerForm(){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState<string|null>(null);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError(null);const f=new FormData(e.currentTarget);const r=await postJson<{producer?:Producer;error?:string}>("/api/producers",Object.fromEntries([...f.entries()].map(([k,v])=>[k,String(v)||null])));setBusy(false);if(!r.producer){setError(r.error||"Could not create producer");return;}e.currentTarget.reset();router.refresh();}return <form className="card form-stack" onSubmit={submit}><h3 className="field-group-title">Add producer</h3>{error&&<p className="err">{error}</p>}<div className="picker-row"><label className="field"><span className="field-label">Chinese name *</span><input className="input" name="name_zh" required/></label><label className="field"><span className="field-label">English name</span><input className="input" name="name_en"/></label><label className="field"><span className="field-label">Email</span><input className="input" name="contact_email" type="email"/></label><label className="field"><span className="field-label">WeChat</span><input className="input" name="contact_wechat"/></label></div><button className="btn btn-primary" disabled={busy}>{busy?"Adding…":"Add producer"}</button></form>}
